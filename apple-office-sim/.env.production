# Se usa automáticamente en `npm run build` (modo producción). No contiene secretos.
# Si al hacer el build ya existe una variable VITE_API_URL en el entorno, esa tiene prioridad.
VITE_API_URL=https://api.appleoffice.com.ar
